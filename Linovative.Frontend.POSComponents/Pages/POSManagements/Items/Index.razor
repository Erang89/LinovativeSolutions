@attribute [Route(PageRoutes.PostManagement.ItemMaster.IndexItems)]
@inject IAppNavigationService _navigationManager;
@code {
    protected override async Task OnInitializedAsync()
    {
        _navigationManager.NavigateTo(PageRoutes.PostManagement.ItemMaster.Item);
        await base.OnInitializedAsync();
    }
}
