@attribute [Route(PageRoutes.PostManagement.PosSettings.Trasactions.Index)]
@inject IAppNavigationService _navigationManager;

@code {
    protected override async Task OnInitializedAsync()
    {
        _navigationManager.NavigateTo(PageRoutes.PostManagement.PosSettings.Trasactions.Payments.PaymentMethod);
        await base.OnInitializedAsync();
    }
}
